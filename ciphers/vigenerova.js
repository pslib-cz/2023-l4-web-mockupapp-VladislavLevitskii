function encrypt() {
    var originalText = document.getElementById("originalText").value.toUpperCase();
    var key = document.getElementById("keyvigener").value.toUpperCase();
    var result = "";
    var keyIndex = 0;

    /*
    
    Váš kód
    
    */

    document.getElementById("resultText").value = result;
}

// Funkce pro dešifrování Vigenèrovy šifry
function decrypt() {
    var originalText = document.getElementById("originalText").value.toUpperCase();
    var key = document.getElementById("keyvigener").value.toUpperCase();
    var result = "";
    var keyIndex = 0;

    /*
    
    Váš kód
    
    */

    document.getElementById("resultText").value = result;
}